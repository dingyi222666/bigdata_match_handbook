## linux常用命令帮助

```shell
jps #查看进程
kill -9 pid # 强制杀死进程 
kill -15 pid # 通知关闭进程
hostnamectl -set-hostname name # 设置主机名字
pwd # 查看当前文件夹位置
cd # 列出到文件夹
ssh-keygen #生成ssh密钥
ll #列出当前文件和文件夹下的东西
mkdir # 创建文件夹
touch # 新建文件
scp #使用ssh复制文件到其他主机
whereis #查找命令的可执行文件的绝对路径
```

